name: Deploy App
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Sync Server
        run: |
          curl -X POST "https://voila-api.mmopro.in/sync-app"             -H "Content-Type: application/json"             -d '{"app": "bbtestaapp-1761151947839", "repo": "https://github.com/BharatSharma34/bbtestaapp-1761151947839.git"}'